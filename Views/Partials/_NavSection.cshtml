@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@using Umbraco.Cms.Core
@using Umbraco.Cms.Core.Models.PublishedContent
@using Umbraco.Cms.Core.Routing
@using Umbraco.Extensions

@inject IPublishedValueFallback PublishedValueFallback
@inject IPublishedUrlProvider PublishedUrlProvider


@{ 
    
    var site = Model.Root();

    var selection = Model.Root().Children.Where(x => x.IsVisible(PublishedValueFallback)).ToArray(); 

 }

@if (selection?.Length > 0)
{
    <ul>
        @foreach (var item in selection)
        {
            <li class="@(item.IsAncestorOrSelf(Model) ? "current" : null)">
                <a href="@item.Url(PublishedUrlProvider)">@item.Name</a>
            </li>
        }
    </ul>
}

<section class="navigation-section">
      
        <div class="nav-container">
          <a class="logo" href="/"><img
            src="mallar sidor\crito_logo.png"
            alt="Crito logotype"
          /></a>
          <nav>
            <div class="first-from-top">
            <div class="contact-company-traditional">
                <span>+46 (8) 121 470 |</span>
                <span>info@crito.com |</span>
                <span>Sveavägen 31, 111 34 STOCKHOLM |
                </span>
            </div>
        </div>

            <div class="contact-company-social-media">
                <a><i class="fab fa-facebook-square"></i></a>
                <a><i class="fa-brands fa-twitter"></i></a>
                <a><i class="fa-brands fa-instagram"></i></a>
                <a><i class="fa-brands fa-linkedin-in"></i></a>
            </div>
       
        <div class="second-from-top">
            <div class="navigation">
                <a>Home</a>
                <a>Service</a>
                <a>News</a>
                <a>Contact</a>
                <a class="login">Login</a>
            </div>
        </div>
        </div>
        </section>